// for backward compatibility, no longer linked from site
import React from 'react';
import Head from '@docusaurus/Head';

export default function Registry() {
  return (
    <Head>
    <meta http-equiv="refresh" content="0;URL='/providers/linode'" />
    </Head>
  );
};