// Sidebar for the query-library docs-plugin instance. Autogenerated from the
// queries/ directory, so entries group by provider then service to match the
// browse IA (landing cards -> provider tables -> query pages) with no
// hand-maintained list.

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  queryLibrarySidebar: [{type: 'autogenerated', dirName: 'queries'}],
};

module.exports = sidebars;
